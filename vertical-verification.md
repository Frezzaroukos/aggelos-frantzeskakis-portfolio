# Vertical Cinematic Portfolio — Verification Notes

The desktop preview now reads as a vertical narrative rather than a horizontal gallery. The opening scene uses the AF flight-path mark, a stronger headline, layered Pegasus artwork, rotating orbit, scroll-linked scale/offset, and a live pointer response. The page then moves through Signal, Selected Work, AXON OS, Method, Repository Library, Profile, and Contact as clear cinematic chapters.

The current GitHub snapshot contains AXON as a private TypeScript repository, Anabasis, Anafora, Thermidor, the profile configuration repository, and the remaining owner repositories. Morfos is excluded from all rendered content. The project cards use repository metadata and curated README-derived facts; private sources are explicitly labeled as private concepts.

The desktop UI includes sticky glass navigation, chapter index with active progress, scroll progress bar, theme toggle, GitHub source link, command-style quick navigation, guide dialog, project deep-dive dialogs, repository filters, copyable handle, and reduced-motion fallbacks. The mobile preview switches naturally to vertical section flow with a compact AF mark, mobile navigation, single-column project cards, readable deep-dive content, and contact/profile continuity.

TypeScript and production build passed. The build retains a non-blocking large JavaScript chunk advisory from the existing bundle. No GitHub repository visibility was changed; the portfolio project remains unchanged in visibility pending the user’s later explicit decision.
